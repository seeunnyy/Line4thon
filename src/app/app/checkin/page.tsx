import ScreenPlaceholder from "@/components/ScreenPlaceholder";

export default function CheckinPage() {
  return (
    <ScreenPlaceholder
      title="재개 체크인"
      description="지금 컨디션에 맞는 복귀 계획을 함께 세워요"
    />
  );
}
