import ScreenPlaceholder from "@/components/ScreenPlaceholder";

export default function ForecastPage() {
  return (
    <ScreenPlaceholder
      title="예보"
      description="다가오는 이벤트를 등록하고 예보를 확인해요"
    />
  );
}
